#! /bin/bash

# FILE env.sh
# ===========
#
# The env.sh file should be generated by cmake !
#
# You can:
#     - source env.sh before using chdb
#     - Integrate the exports to your .bashrc
#     - Integrate the exports to a module file
#

export CHDB_MPI_CMD="/bin/bash -c 'ssh \$(hostname) BINDIR/slave-%CHDB_MPI%-wrapper.sh \$(pwd) \$(BINDIR/mod-wrapper.sh) \$(BINDIR/env-wrapper.sh) \$(BINDIR/snippet-wrapper.sh) %MPI_SLAVES% %HOSTNAME% %COMMAND% ' "
export PATH=BINDIR:$PATH
